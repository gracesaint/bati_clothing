<%= form_with(model: order_item, local: true) do |form| %>
  <% if order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
      <% order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <hr>

  <!-- div class="field">
    < form.label :product_id %>
    <  form.number_field :product_id, id: :order_item_product_id %>
  </div -->
  
  <h2><%= order_item.product.title %></h2>

  <!-- div class="field">
    < form.label :order_id %>
    < form.number_field :order_id, id: :order_item_order_id %>
  </div -->

  <div class="form-group">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :order_item_quantity, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
