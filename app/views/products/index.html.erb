<p id="notice"><%= notice %></p>


<div class="jumbotron">
  <h1>Handcrafted Items from Around the Globe</h1>
  <h2>Discover one-of-a-kind clothes</h2>
</div>


<h1 style="text-align: center;">Women's Clothing</h1>


<div class="row">
  <% @products.each do |product| %>
    <div class="col-md-6" style="text-align: center;">
    <div id="productName"><h1><%= link_to product.title, product %></h1></div>
    <%= link_to product_path(product) do %>
      <%= image_tag product.image_url %></br>
    <% end %>
    <%= print_price(product.price) %></br>
    <strong>Color: </strong><%= product.color %></br>
    <%= button_to "Add to Cart", order_items_path(product_id: product.id), class: "btn btn-primary" %></br>
    <br>
    
    <%  if current_user.admin? && !current_user?(user) %>
        <%= link_to 'Edit', edit_product_path(product) %></br>
        <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></br>
    <% end %>
    
    </div>
  <% end %>
</div>

<!-- table id= "products" style="margin-left: 30px; border-collapse: collapse;" border="0"; cellspacing="0";>
  <thead>
    <tr>
      <th>Title</th>
      <th>Price</th>
      <th>Color</th>
      <th>Size</th>
      <th>Description</th>
      <th>Image</th>
      <th>Buy</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody style="border-left: 0px none; border-right: 0px none;">
    < @products.each do |product| %>
      <tr>
        <td>< link_to product.title, product %></td>
        <td>< print_price(product.price) %></td>
        <td>< product.color %></td>
        <td>= product.size %></td>
        <td>< product.description %></td>
        <td>< image_tag "products/#{product.image_url}" %></td>
        <td>< link_to 'Show', product_path(product) %></td>
        <td>< button_to "Add to Cart", order_items_path(product_id: product.id) %></td> -->
        
        <%  if current_user.admin? %>
        <%= link_to 'Edit', edit_product_path(product) %></br>
        <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></br>
        <% end %>
        
     <!--    
      </tr>
    < end %>
  </tbody>
</table -->

<br>



<%= link_to 'Create a New Product', new_product_path, id: "new-product" %>

